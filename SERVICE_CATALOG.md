# 137 Issabel Bridge

PHP package deployed **on Issabel** (no own DB/domain). Uploads call recordings to File Storage, creates `PhoneCall` requests on 137-request, returns `trackingCode`. AMI helpers for operator answer/reject.

See `README.md` (Persian) for install and employer task mapping.
