; =============================================================================
; Asterisk HTTP + WebSocket for JsSIP (WSS on 8089 is on PJSIP transport;
; HTTP server still needed for Asterisk WS upgrade on some setups).
; Merge into /etc/asterisk/http.conf / rtp.conf as documented in INSTALL.md
; =============================================================================

; --- http.conf ---
;[general]
;enabled=yes
;bindaddr=0.0.0.0
;bindport=8088
;tlsenable=yes
;tlsbindaddr=0.0.0.0:8089
;tlscertfile=/etc/asterisk/keys/asterisk.crt
;tlsprivatekey=/etc/asterisk/keys/asterisk.key

; --- rtp.conf (ICE/WebRTC) — بدون این، تماس سیگنال می‌خورد ولی صدا/بوق می‌ماند ---
;[general]
;rtpstart=10000
;rtpend=20000
;icesupport=yes
;stunaddr=stun.l.google.com:19302
;; اگر ایزابل چند کارت شبکه دارد، آدرس LAN را صریح بگذار:
;stun_blacklist=0.0.0.0/0
;; یا در PJSIP endpoint: media_address=192.168.1.70
